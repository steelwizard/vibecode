; Configuration for 0:\SYSTEM.INI — see README.md

[keyboard]
layout=de

[video]
mode=1080p

; Colon-separated directories searched for .COM programs (current drive).
; Absolute paths only — no drive letters (e.g. \FOS:\BIN).
[shell]
path=\FOS
